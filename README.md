# Лабораторная работа на stack и deque.
`include "deq_and_stack.h`
Взять реализации
`cart_collector.c` основная функция
`time_test`.c измерение времени работы структур
компиляция программы: 
`gcc cart_collector.c -g -O3 -o cart_collector`
`./cart_collector input.txt`
или с просмотром действий
`./cart_collector input.txt -v`